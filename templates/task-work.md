<!--
작업 문서다. `worklog/<repo>/tasks/<대상 브랜치>.work.md`가 이 파일이다.
보고서와 짝이므로 항상 함께 만든다. 만들 때는 `## 작업 목록`만 채우고 나머지는 자리 표시자로 둔다.

배경과 목적은 쓰지 않는다. 같은 이름의 보고서(`<대상 브랜치>.md`)가 그것을 갖고 있다.

골격
- 아래 `## ` 제목 4개만 쓴다. 순서를 바꾸지 않고, 새 제목을 만들지 않는다.
- `## 진행`은 세션마다 덧붙인다. 앞 회차의 기록은 고치지 않는다.
- 작업 목록의 항목은 무엇을 어디서 하는지 한 줄이다. 설명은 보고서에 있다.
- 행 번호는 드리프트하므로 `파일:심볼`로 쓴다.
-->

# <보고서와 같은 제목> — 작업

## 작업 목록

- [ ] <할 일> (`path/to/file.ts`)

## 진행

### <YYYY-MM-DD HH:MM>

- 한 것:
- 막힌 것:
- 다음 할 것:

## 검증

<무엇을 어떻게 실행해서 어떤 결과가 나왔는가. 실패하거나 건너뛴 항목도 적는다.>

## 완료

<사용자가 통과로 확인한 뒤에 쓴다. 커밋·푸시 상태와 남은 일.>
