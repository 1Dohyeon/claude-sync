<!--
사람이 읽는 보고서다. `worklog/<repo>/tasks/<대상 브랜치>.md`가 이 파일이다.
승인받고, 인수인계하고, 나중에 "이게 뭐였지"를 확인하는 일을 이 문서 하나로 끝낸다.
진행 로그와 체크리스트는 여기가 아니라 같은 이름의 `.work.md`에 쓴다.

골격
- 아래 `## ` 제목 5개만 쓴다. 순서를 바꾸지 않고, 새 제목을 만들지 않는다.
- 해당 없는 섹션은 통째로 지운다. 제목만 남겨두지 않는다.
- `## 진행 상황`·`## 정정`·`## 후속 수정` 같은 섹션을 덧붙이지 않는다.

쓰지 않는 것
- 체크박스, 커밋 해시, 행 번호, 검증 로그, 파일 경로 나열
- 시각 표기와 "지금은 푸시 안 함" 같은 시점 정보

사실이 바뀌면
- 덮어쓴다. 정정 문장을 덧붙이지 않는다. 이 문서는 항상 현재의 계획 하나만 보여준다.

문장
- 산문으로 쓴다. 문단은 2~3줄, 불릿은 한 문장.
- 식별자를 산문에 박지 않는다. 꼭 필요한 이름 하나까지가 한도다.
-->

# <무엇을 하는 작업인가>

<이 작업이 무엇인지 두 줄로. 제목만 보고는 모를 사람에게 설명하듯이 쓴다.>

| 상태 | 브랜치 | 범위 |
|---|---|---|
| <설계 중 / 구현 중 / 검증 대기 / 끝> | `<브랜치>` | <어디까지가 이 작업인가> |

## 지금 무엇이 문제인가

<왜 이 작업을 하는가. 현재 동작이 어떻게 불편하거나 틀렸는지 사실로 쓴다.>

## 어떻게 바꾸는가

<무엇을 어떻게 고치는가. 구현 순서가 아니라 바뀌는 동작을 쓴다.>

<검토했지만 택하지 않은 방법이 있으면 왜 버렸는지 한 문단으로 남긴다.>

## 어떤 효과가 있는가

<끝나면 무엇이 달라지는가. 그리고 무엇이 그대로인가.>

## 함께 손대는 곳

<이 변경 때문에 같이 고쳐야 하는 곳. 공용 코드, 방어로직, 다른 화면 등.>

## 확인 방법

<무엇을 해보면 끝났다고 판정할 수 있는가.>
